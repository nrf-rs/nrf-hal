language: rust
rust:
  - nightly
cache: cargo
addons:
  apt:
    packages:
      - gcc-arm-none-eabi
script:
  - rustup target add thumbv7em-none-eabihf
  - cargo build --verbose --manifest-path nrf52-hal/Cargo.toml
  - cargo build --verbose --manifest-path nrf52840-hal/Cargo.toml
